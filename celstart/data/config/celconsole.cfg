CelConsole.Bind.Open = tab
CelConsole.Bind.List = ctrl-shift-e
; When monitor is true we will monitor the entity creation and removal so that
; it is easier to find leaks. Because this adds some overhead at runtime for
; all CEL apps (when creating and especially removing entities) this is not
; enabled by default.
;CelConsole.Monitor = true
